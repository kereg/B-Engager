'use strict';

describe('fullAnalysis module', function() {

  beforeEach(module('fullAnalysis'));

  describe('fullAnalysis controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('fullAnalysisCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});