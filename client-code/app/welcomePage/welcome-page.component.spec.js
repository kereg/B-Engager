'use strict';

describe('welcomePage module', function() {

  beforeEach(module('welcomePage'));

  describe('welcomePage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('welcomePageCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});