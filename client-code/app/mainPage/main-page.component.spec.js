'use strict';

describe('mainPage module', function() {

  beforeEach(module('mainPage'));

  describe('mainPage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('mainPageCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});