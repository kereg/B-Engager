'use strict';

describe('comments module', function() {

  beforeEach(module('comments'));

  describe('comments controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('commentsCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});